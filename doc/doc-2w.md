---
theme: "simple"
customTheme : "my-theme"
transition: "convex"
slideNumber: false
title: "コーディング 2nd Season 1w - JavaScript"
---

# コーディング  2nd Season
## 2週目「JavaScript」
<img src="./img/logo_bg_none.png" style="width: 16%;">

---

1. 受講生MTG
1. はじめに
1. 座学
1. 実習
1. 講師FB
1. まとめ
1. 受講生MTG

note:
13:30～13:35 05分＞生徒の打ち合わせ
13:35～13:40 05分＞イントロ
13:40～14:05 40分＞座学
14:10～14:50 40分＞作業
14:50～15:05 15分＞FB
15:05～15:25 10分＞締め
15:25～15:30 05分＞生徒の打ち合わせ

---

## はじめに

--

### 先週の課題発表
「お気に入りサービスを3分で紹介」

---

座学の時間
## JavaScript

---

### 変数スコープ

--

スコープとは参照できる範囲のこと

--

ところで“変数宣言”何があった？

--

宣言 | 内容
-------------|---------------
何もつけない      | グローバル
var      | 関数　再宣言可
let      | ブロック　再宣言不可
const      | ブロック　再宣言不可　定数
[変数宣言の種類]

--

スコープの種類

```txt
├── グローバルスコープ
└── ローカルスコープ
    ├──関数スコープ
    └──ブロックスコープ
```

---


配列




```js

let a = 1;
let b = [1,2,3];

```



変数の1つ
複数の値をまとめて管理、格納できる

<img src="./img/2w/ishou.jpg" style="width: 16%;">

衣装ケース

Tシャツ
パンツ
靴下

※1つの段にはひとつしかもの（値）が入らないよ

配列名 = [要素1, 要素2, 要素3,　…];
letterCase = ["Tシャツ", "パンツ", "靴下"]; 

配列名[添え字];
<letterCase 0=""></letterCase>

連想配列
JSには厳密にはない。
オブジェクトという


Object.keys()


配列名.length
配列名.push("電池");
配列名.pop();

push()
配列の最後に要素を追加します。
pop()
配列の最後の要素を削除します。
unshift()
配列の最初に要素を追加します。
shift()
配列の最初の要素を削除します。
slice()
配列をコピーするときなどに使用します。
splice()
配列の任意の位置で要素の追加や削除を行えます。


a   = [56,3,62,17,87,22,36,83,21,12];
sum = a[3] + a[7];
document.write(sum);


a   = [56,3,62,17,87,22,36,83,21,12];
sum = 0;

for (i = 0; i < a.length; i++) {
    sum += a[i]; // sum = sum + a[i];でも可。
}
document.write(sum);

//「forEach」で試した例
[1,2,3].forEach(function( value ) {
 
    console.log( value );
 
});
 
 
//「map」で試した例
[1,2,3].map(function( value ) {
 
    console.log( value );
 
});

API

関数
アロー関数




クリエイティブのアドバイス

作るときには設計を

サイコロアプリ
おみくじ
アクセスするたびに違うメッセージ

ランダム

クリックゲーム
変数イベント


---

## 来週に向けて

--

自分のお気に入りサービスを見つけて、3分で紹介

--

### 予告

次週からは、大枠では

- HTML
- CSS
- JavaScript

と回を分けますが、どうしても全部からんでくる。

というわけで、「それだけ」とはならないと思います


